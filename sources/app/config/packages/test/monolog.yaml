monolog:
    handlers:
        main:
            type: stream
            path: "php://stderr"
#            path: "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
            channels: ["!event"]
